name: Smoke test

on:
  push:

jobs:
  build_image:
    name: Build Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        name: Checkout repo
      - run: docker build -t kartkowka .
